{
  "name": "trikhub",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "TrikHub SDK - Secure trik execution for AI agents",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Molefas/trikhub.git"
  },
  "bugs": {
    "url": "https://github.com/Molefas/trikhub/issues"
  },
  "homepage": "https://github.com/Molefas/trikhub#readme",
  "author": "Molefas",
  "license": "MIT",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf packages/js/*/dist packages/js/*/*.tsbuildinfo",
    "typecheck": "tsc -b --noEmit",
    "test": "vitest run tests/js",
    "test:e2e": "vitest run tests/e2e/cli-install.e2e.test.ts",
    "test:e2e:lifecycle": "vitest run --config tests/e2e/lifecycle/vitest.config.ts",
    "test:all": "vitest run",
    "test:watch": "vitest",
    "lint:trik": "node packages/js/linter/dist/cli.js",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "pnpm build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "@changesets/changelog-github": "^0.5.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^3.2.0"
  }
}
