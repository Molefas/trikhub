# LLM Configuration (set at least one API key)
# The agent will auto-detect the provider based on which key is set
# Priority: Anthropic > Google > OpenAI

OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# GOOGLE_API_KEY=...

# Optional: Force a specific provider (overrides auto-detection)
# LLM_PROVIDER=openai

# Optional: Override the default model for the selected provider
# LLM_MODEL=gpt-4o

# Optional: Python path for Python triks (if using any)
# TRIKHUB_PYTHON=/usr/local/bin/python3
