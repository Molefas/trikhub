import { describe, it, expect } from 'vitest';

describe('smoke', () => {
  it('packages build successfully', () => {
    // Placeholder: v1 tests were stripped in P1, v2 test suite is pending.
    // CI validates the build step; this ensures vitest finds at least one test.
    expect(true).toBe(true);
  });
});
